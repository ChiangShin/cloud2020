spring:
  application:
    name: hystrix-dashboard
server:
  port: 8900
eureka:
  client:
    serviceUrl:
      defaultZone: http://root:123456@localhost:8761/eureka/
feign:
  hystrix:
    enabled: true